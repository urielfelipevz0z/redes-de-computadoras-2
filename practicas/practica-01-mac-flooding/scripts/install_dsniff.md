# Comandos de Instalación de dsniff

## Ubuntu/Debian (APT)
```bash
# Actualizar repositorios
sudo apt update

# Instalar dsniff
sudo apt install dsniff -y

# Verificar instalación
dpkg -l | grep dsniff
which macof
macof -h
```
